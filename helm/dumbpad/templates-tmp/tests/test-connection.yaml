apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dumbpad.fullname" . }}-test-connection"
  labels:
    {{- include "dumbpad.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dumbpad.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
